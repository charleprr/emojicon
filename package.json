{
  "name": "emojicon",
  "version": "2.1.1",
  "description": "A Discord bot that converts images into emojis!",
  "author": "charleprr",
  "license": "LGPL-3.0-only",
  "keywords": [
    "discord",
    "emojis",
    "images",
    "bot"
  ],
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "start": "rm -rf ./dist && tsc && node dist/index.js",
    "test": "mocha -r ts-node/register tests/**/*.spec.ts -s 750"
  },
  "dependencies": {
    "discord.js": "^14.13.0",
    "jimp": "^0.22.5",
    "multidither": "^1.0.5"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.3",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
